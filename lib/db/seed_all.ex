defmodule GladiatorPlane.Repo.Seed do
  alias GladiatorPlane.Repo
  import GladiatorPlane.WarriorGen

  def seed_all do
    # Enum.each(0..20, fn _ -> Repo.insert(random_warrior_struct()) end)
  end
end
